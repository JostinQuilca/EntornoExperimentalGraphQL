datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Resena {
  id         String @id @default(uuid())
  comentario String
  usuarioId  String
  productoId String
}
